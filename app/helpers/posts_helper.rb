module PostsHelper

	# def user_name(post)
	# 	post.present? and post.user.name.present? ? "#{ (post.user.name).capitalize}" : ''
	# end


	# def title(post)
	# 	post.present? ? "#{(post.title).capitalize}" : ""
	# end

end
